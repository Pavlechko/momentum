export type BackgroundData = {
    alt: string,
    image: string,
    photographer: string,
    source: string,
    source_url: string,
}

export type Background = {
    Pexels: BackgroundData,
    Unsplash: BackgroundData,
}